# Copia este archivo como .env y completa los valores reales.
# .env NUNCA se sube al repo (está en .gitignore).
MONGODB_URI=mongodb+srv://USUARIO:PASSWORD@cluster.xxxxx.mongodb.net/?appName=tablero-kanban
DB_NAME=tablero_kanban
